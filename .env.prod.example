# Production Docker Compose Environment
# Copy this file to .env.prod and fill in your production values
# These variables are used by docker-compose.prod.yml

# ========================================
# Database Configuration
# ========================================
# Optional: Customize database name and user (defaults: moneyy, postgres)
# Recommended: Use custom values for production security
DB_NAME=moneyy
DB_USER=postgres

# REQUIRED: Use a strong password in production
DB_PASSWORD=your_secure_production_password_here

# ========================================
# Encryption (REQUIRED)
# ========================================
# Master encryption key for sensitive data
# IMPORTANT: Generate a secure key with: openssl rand -base64 32
ENC_MASTER_KEY=your_production_encryption_key_here

# ========================================
# Authentication (Passkey/WebAuthn)
# ========================================
# JWT Secret for token signing
# IMPORTANT: Generate a secure secret with: openssl rand -base64 32
JWT_SECRET=your_production_jwt_secret_min_32_chars_here

# WebAuthn Configuration
# RP_ID should be your domain (without protocol or port)
# Example: yourdomain.com
WEBAUTHN_RP_ID=yourdomain.com

# RP_ORIGIN should be the full URL where users access your app
# Example: https://yourdomain.com
WEBAUTHN_RP_ORIGIN=https://yourdomain.com

# ========================================
# Server Configuration
# ========================================
# CORS allowed origins (comma-separated)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# ========================================
# Docker Configuration
# ========================================
# Docker image version tag (use specific version tags in production, not "latest")
# Example: v1.0.0
VERSION=latest
