# Moneyy Production Environment Variables
# Use these when running the Docker container
#
# Example:
# docker run -d \
#   --name moneyy \
#   -p 4000:4000 \
#   -v moneyy-data:/app/data \
#   --env-file .env.prod \
#   ghcr.io/saswatds/moneyy:latest

# ========================================
# REQUIRED Variables
# ========================================

# Master encryption key for sensitive data
# Generate with: openssl rand -base64 32
ENC_MASTER_KEY=your_production_encryption_key_here

# JWT Secret for token signing (min 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_production_jwt_secret_min_32_chars_here

# WebAuthn Relying Party ID (your domain without protocol/port)
# Example: yourdomain.com
WEBAUTHN_RP_ID=yourdomain.com

# WebAuthn Relying Party Origin (full URL where users access your app)
# Example: https://yourdomain.com
WEBAUTHN_RP_ORIGIN=https://yourdomain.com

# ========================================
# Optional Variables (have defaults)
# ========================================

# SQLite database path (default: /app/data/moneyy.db)
# DB_PATH=/app/data/moneyy.db

# Server port (default: 4000)
# SERVER_PORT=4000

# CORS allowed origins (default: *)
# Recommended: Set specific origins in production
# CORS_ORIGINS=https://yourdomain.com

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=info

# Log format: json, text (default: json)
# LOG_FORMAT=json
