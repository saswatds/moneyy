# Docker Compose Development Environment
# Copy this file to .env and fill in the values
# These variables are used by docker-compose.yml

# ========================================
# Database Configuration
# ========================================
# Database password (required, no default)
# Defaults: DB_NAME=moneyy, DB_USER=postgres
DB_PASSWORD=postgres

# ========================================
# Encryption (REQUIRED for sync features)
# ========================================
# Master encryption key for sensitive data
# For Docker dev: use the example below
# For production: Generate with: openssl rand -base64 32
ENC_MASTER_KEY=dev-encryption-key-change-in-production

# ========================================
# Authentication (Passkey/WebAuthn)
# ========================================
# JWT Secret for token signing (must be 32+ characters)
# For Docker dev: use the example below
# For production: Generate with: openssl rand -base64 32
JWT_SECRET=dev-secret-min-32-characters-long-change-in-production

# WebAuthn Configuration
# RP_ID should match your domain (for local dev: localhost)
WEBAUTHN_RP_ID=localhost

# RP_ORIGIN should match the URL where users access the app
# For docker-compose dev: http://localhost:5173 (frontend dev server)
WEBAUTHN_RP_ORIGIN=http://localhost:5173
