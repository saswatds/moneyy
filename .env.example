# Money App Configuration (Passkey Authentication)
# Copy this to .env and fill in the values

# ========================================
# Database Configuration
# ========================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=moneyy
DB_SSLMODE=disable

# ========================================
# Server Configuration
# ========================================
SERVER_PORT=4000
CORS_ORIGINS=http://localhost:5173

# ========================================
# Authentication - JWT (REQUIRED)
# ========================================
# Must be at least 32 characters
# For Docker dev: use the default below
# For production: Generate with: openssl rand -base64 32
JWT_SECRET=dev-secret-min-32-characters-long-change-in-production

# ========================================
# Authentication - WebAuthn/Passkey (REQUIRED)
# ========================================
# Relying Party ID - must match your domain
# For local dev: localhost
# For production: yourdomain.com
WEBAUTHN_RP_ID=localhost

# Relying Party Origin - must match your URL
# For local dev: http://localhost:4000
# For production: https://yourdomain.com
WEBAUTHN_RP_ORIGIN=http://localhost:4000

# ========================================
# Session Configuration (OPTIONAL)
# ========================================
# Session duration in hours (default: 168 = 7 days)
SESSION_DURATION_HOURS=168

# ========================================
# Encryption (REQUIRED for sync features)
# ========================================
# Master encryption key for sensitive data
# For Docker dev: use a simple key below
# For production: Generate with: openssl rand -base64 32
ENC_MASTER_KEY=dev-encryption-key-change-in-production
